language: node_js

env:
  matrix:
    - M1=ONE
    - M2=TWO
   


build:
  ci:
    - echo "lang c"
    - secs=1; SECONDS=0; while (( SECONDS < secs )); do sleep 1; echo $SECONDS; done

  post_ci:
    - echo "true6"
