language: node_js

env:
  matrix:
    - M1=ONE

build:
  ci:
    - if [ "$(($BUILD_NUMBER % 2))" -eq 0 ]; then failing it; fi # fail if build number is odd
    - printenv

  post_ci:
    - echo "true6"
